# Неизменяемый снимок champion v2

Эта папка содержит веса, SHA-256, runtime-выбор моделей и сырые PAPER/LIVE события на 2026-09-07T17:35:44Z.

Важно: записанный LIVE PnL ещё не учитывал taker fee CLOB V2. Для примера `5 @ 0.500` реальная полная стоимость равна `$2.5875`, потому что feeSchedule рынка равен `rate=0.07, exponent=1, takerOnly=true`. Это не скрытый slippage: CLOB fill был `0.500`, а Polymarket UI показал эффективую цену с fee `0.5175`.

Смотрите `snapshot.json` для манифеста и `artifacts/` для весов. Секретов в снимке нет.
